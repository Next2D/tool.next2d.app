/**
 * @description 全て上書きするボタンのElement IDの名前
 *              Name of the Element ID of the button to override all
 *
 * @type {string}
 * @public
 */
export const $CONFIRM_MODAL_ALL_OVERWRITING_ID: string = "confirm-all-overwriting";

/**
 * @description 全てキャンセルするボタンのElement IDの名前
 *              Name of the Element ID for the Cancel All button
 *
 * @type {string}
 * @public
 */
export const $CONFIRM_MODAL_ALL_CANCEL_ID: string = "confirm-all-cancel";

/**
 * @description 単体の上書きするボタンのElement IDの名前
 *              Name of the Element ID of the button to be overwritten for a single override.
 *
 * @type {string}
 * @public
 */
export const $CONFIRM_MODAL_OVERWRITING_ID: string = "confirm-overwriting";

/**
 * @description 単体のキャンセルするボタンのElement IDの名前
 *              Name of the Element ID of the button to cancel the stand-alone
 *
 * @type {string}
 * @public
 */
export const $CONFIRM_MODAL_CANCEL_ID: string = "confirm-cancel";

/**
 * @description 変更前の画像表示Element IDの名前
 *              Name of image display Element ID before change
 *
 * @type {string}
 * @public
 */
export const $CONFIRM_MODAL_BEFORE_PREVIEW_ID: string = "confirm-before-preview";

/**
 * @description 変更後の画像表示Element IDの名前
 *              Name of the modified image display Element ID
 *
 * @type {string}
 * @public
 */
export const $CONFIRM_MODAL_AFTER_PREVIEW_ID: string = "confirm-after-preview";

/**
 * @description 現在のインスタンス名のInput Element IDの名前
 *              Name of the Input Element ID of the current instance name
 *
 * @type {string}
 * @public
 */
export const $CONFIRM_MODAL_FILE_NAME_ID: string = "confirm-file-name";
