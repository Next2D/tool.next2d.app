/**
 * @description アイテムの同名時のエラーテキスト
 *              Error text when the same name is used for an item
 *
 * @type {string}
 * @constant
 */
export const $ERROR_DUPLICATE_NAME_TEXT: string = "同名のアイテムが存在します";

/**
 * @description シンボルの同名時のエラーテキスト
 *              Error text when symbols have the same name
 *
 * @type {string}
 * @constant
 */
export const $ERROR_DUPLICATE_SYMBOL_TEXT: string = "同名のシンボルが存在します";

/**
 * @description 上書き時のファイル名が空の時のエラーテキスト
 *              Error text when file name is empty when overwriting
 *
 * @type {string}
 * @constant
 */
export const $ERROR_EMPTY_FILE_NAME_TEXT: string = "ファイル名は必須項目です";